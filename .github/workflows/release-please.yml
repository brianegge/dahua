name: release-please

on:
  push:
    branches: [main]
  schedule:
    # Daily at 13:17 UTC. Picks up the release PR created/updated by
    # push runs and merges it, producing at most one release per day.
    - cron: "17 13 * * *"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  release-please:
    runs-on: ubuntu-latest
    steps:
      - id: release
        uses: googleapis/release-please-action@v5
        with:
          # Fine-grained PAT so the release PR triggers other workflows
          # (GITHUB_TOKEN-authored PRs are blocked from triggering Actions).
          token: ${{ secrets.RELEASE_PLEASE_TOKEN }}
          config-file: release-please-config.json
          manifest-file: .release-please-manifest.json

      # On the daily schedule, enable auto-merge on the open release PR.
      # GitHub will merge it once all required status checks pass, so a
      # release is cut at most once per day AND only if CI is green.
      - name: Enable auto-merge on release PR (daily)
        if: github.event_name == 'schedule' && steps.release.outputs.pr != ''
        env:
          GH_TOKEN: ${{ secrets.RELEASE_PLEASE_TOKEN }}
          PR_JSON: ${{ steps.release.outputs.pr }}
        run: |
          PR_NUMBER=$(echo "$PR_JSON" | jq -r '.number')
          echo "Enabling auto-merge on release PR #$PR_NUMBER"
          gh pr merge "$PR_NUMBER" --auto --squash --delete-branch
